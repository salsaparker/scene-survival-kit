<h1> NEW PROFILE </h1>
<%= form_for @profile do |f| %>
	<br/>
	<b>I am a...<b><br/>
	<b>Display Name:</b><br/>
	<%= f.text_field(:name)%>
	<br/>
	<%= f.label(:phone_number)%><br/>
	<%= f.text_field(:phone_number)%>
	<br/>
	<%= f.label(:bio)%> <br/>
	<%= f.text_area(:bio)%>
	<br/>
  <%= f.label(:image)%>
	<%= f.file_field(:image)%>
	<br/>
	<%= f.submit("Submit")%>
<%end%>

